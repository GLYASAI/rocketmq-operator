apiVersion: v1
kind: Service
metadata:
  name: rocketmq-console-nodeport
spec:
  ipFamilies:
  - IPv4
  ipFamilyPolicy: SingleStack
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: rocketmq-console
  type: NodePort